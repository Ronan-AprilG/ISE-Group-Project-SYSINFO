import javax.swing.*;
import java.awt.*;

public class memMenu {
    //declare constants
    public static final int WINDOW_HEIGHT = 360;
    public static final int WINDOW_WIDTH = 640;

    static JPanel initMemMenu()
    {
        memInfo mem = new memInfo();
        JPanel p = new JPanel();

        return p;
    }
}